# sudo: required
# services:
#   - docker

# before_install:
#   - docker build -t rahulrao/frontend-test -f ./frontend/Dockerfile.dev ./frontend

# script:
#   - docker run -e CI=true rahulrao/frontend-test npm test
